<div align="center">

# 群像本

**群聊，不应该是一个ai扮演所有**

📱 跑在你手机里的 AI 群聊角色扮演引擎 · 🔒 安全 · 🧩 本地

> Local-first multi-character AI group-roleplay engine — characters know only what they should, remember verbatim, and take turns under an auditable judge on a user-built scene map. Runs fully offline, on-device.

[快速开始](#快速开始) · [它解决了什么](#它解决了什么) · [为什么它能做到](#为什么它能做到) · [系统手册](docs/SPEC.md)

</div>

---

## 它解决了什么

玩过 AI 群聊/多角色对话的人，多少都遇到过的问题：

- **你想让甲回答，答话的却是丙。** 你明明在跟一个人说话，另一个角色抢着接茬；你想听的那个人，一场戏下来没开口过。
- **耳语变成广播。** 你和一名角色单独说秘密，但是所有角色共用统一上下文，全场角色全听见了，只是在装没听见，几轮过去后彻底装不住了。
- **记忆是编的。** 角色"记得"的事，其实是模型自己脑补的总结版。二十轮之后设定漂移、人设走形，而且**你看不见它记错了什么，也撤不回**。
- **后台是黑箱。** 谁接话、谁在场、谁、该知道什么——你没办法决定，角色出错想改都改不了。
- **数据在别人手里。** 对话、人设、你深夜写的每一段台词，都有可能躺在别人的服务器上。

**群像本把这些问题当工程问题修掉了**，不是靠把提示词写长一点。

| 你遇到的 | 群像本的做法 |
|---|---|
| 总有人抢戏 | 每轮一次结构化判定决定"谁输出下一段内容"；拿不准就**把发言权还给你**，绝不抢戏 |
| 耳语泄露 | 每条消息独立判定知情名单，每个角色独立记忆——谁听得见，谁的上下文里才有这句；听不见的，**记忆里也没有**，从根源杜绝记忆错乱 |
| 角色总出现在不该在的地方 | 地图机制：世界由固定场景文件构成（名称+描述，全量注入角色与判定层），在场 = 位置相同的**代码事实**；换场景只认明确描写（极严苛判定）或定位针手选 |
| 记忆漂移 | 角色记忆是**逐字原文移植**，不是模型总结——没有可编造的环节 |
| 判断是黑箱 | 每一次判定全程落盘成日志：每道题的原始概率、走了哪条路径、为什么回退，随时可查可审计 |
| 数据不可控 | 纯文件存储，无数据库、无账号、无云端；数据在你设备上，格式看得懂，坏了能修，不存在远程后端服务器 |

还有一个可能被低估的点：**被偷听的人，不知道自己被偷听。** 单向感知层的存在只对你和判定层可见——现场角色感知不到偷听者，这正是这类剧情成立的全部前提。

---

## 为什么它能做到

上面那张表里的每一行，背后都不是"提示词调优"，是一套**判定与生成分离的运行时**：

```
                         ┌──────────────────────────┐
   你的手机 / 浏览器 ───▶ │  前端（移动优先，微信式） │
                         └────────────┬─────────────┘
                                      │ /api（NDJSON 流式）
                         ┌────────────▼─────────────┐
                         │  群聊引擎 GroupSession    │
                         │  一轮：判定 → 落盘 → 生成 │
                         └───┬──────────────────┬───┘
                             │                  │
              ┌──────────────▼──────┐   ┌───────▼────────────────┐
              │ 判定层（快，结构化）│   │ 生成层（慢，流式）     │
              │ 谁接话 / 谁在场     │   │ 角色台词               │
              │ 谁听得见 / 转告     │   │ 后台记账（状态/场景）  │
              │ 状态门              │   │ 现场所见白描           │
              └──────────┬──────────┘   └───────┬────────────────┘
                         └──────────┬───────────┘
                                    ▼
              ┌───────────────────────────────────────────┐
              │ 数据：纯文件（剧情.jsonl 为唯一事实源）    │
              │ 角色.md 性格.md 人物关系.md 状态.yaml 记忆.jsonl │
              │ 判定.jsonl（只给人看，永不进模型）         │
              └───────────────────────────────────────────┘
```

几个关键词，供检索与评估：

- **判定/生成分离**：一次廉价的结构化判定（TypeSafe System One 协议的 noul/choice 类型化问题）同时回答"谁接话、三层场景变动、谁能感知这条消息、是否在转告、状态要不要重记"；生成模型只负责把角色演好。判定失败三级回退，剧情不中断。
- **知情边界（knowledge boundary）**：每条消息出生即带 `visible_to` 快照，上下文窗口与记忆账本从第一刻一致；接入/偷听者各带感知起点锚（`since`），接入之前的事不可能是他的记忆。
- **唯一事实源 + 重放**：`剧情.jsonl` append-only 账本，状态/记忆/在场全部可由它重放重建（`pnpm rebuild`）；改写与删除是物理语义，"当前上下文"与"角色知道什么"永不分叉。
- **地图机制**：世界由一张固定地图构成——场景文件（名称+描述，一经创建不可改名不可删除）全量注入角色与判定层；在场 = 位置相同的代码事实，换场景只认明确描写或定位针手选，每个角色的位置逐轮落在 presence 行上，可审计可重放。
- **两段式事件补全**：离场者回归时，先从对话依据中发现事件骨架，再按**每个参与者各自的限知视角**渲染成记忆——同一场事，两个人的记忆版本视角不同、事实不矛盾。
- **状态账本七字段整体快照**：生理/心理/外观/位置/性格演变/姓名变化/人物关系变化——只保留最新版，杜绝堆叠污染；写作被硬约束为客观骨架，防复读。
- **全链路判定日志**：每道判定题的原始概率分布、耗时、回退原因全部落盘，`判定.jsonl` 只给人看、永不进入任何模型输入。

全部行为有回归钉：八组离线自检（无需任何 API 密钥即可运行）+ 在线验收矩阵，覆盖账本幂等、可见性过滤、逐字移植、阈值回退、物理改删、重放重建。**这个仓库的行为是可验证的，不是"作者说它行"。**

---

## 快速开始

**电脑上跑（最省事）**

```bash
pnpm install
pnpm host        # 后端：http://127.0.0.1:8787
pnpm web:dev     # 前端：http://localhost:5173
```

Windows 下直接双击 `启动.bat`。

**手机上跑（完全自包含）**

安卓端把 Node 运行时、引擎、HTTP 服务、前端界面全部打进一个 APK，跑在应用私有目录、只绑回环地址、通知栏无常驻项——你的存档和密钥不必经过任何人。构建流程见 [`docs/安卓应用说明.md`](docs/安卓应用说明.md)。

**配什么才能用**

进「模型配置」填一个 OpenAI 兼容的对话模型（地址 / 模型 ID / 密钥）即可开跑；可选配一个判定模型密钥启用快路径——不配也能跑，引擎自动回退到完整总管判定。

**文档**

- [`docs/SPEC.md`](docs/SPEC.md) —— 系统手册：运行时形态、数据格式、判定协议、接口契约、已知限制（AI 助手接入本项目前建议先读）
- [`docs/安卓应用说明.md`](docs/安卓应用说明.md) —— 安卓自包含构建与数据迁移
- [`docs/前端设计说明.md`](docs/前端设计说明.md) —— 前端设计决策

---

## 设计思路

最初只是一个朴素的问题：**为什么 AI 群聊里的角色总是"全知"的？**

所有人听见所有话、所有人记得所有事、离场的人回来毫无损失——这不是"模型不够聪明"，是**架构根本没给"不知道"留位置**。于是这个项目从第一天起就围绕一件事构建：知情边界。消息有可见范围、记忆有移植来源、场景有层级、离场有时间锚——"不知道"在系统里是一等公民，不是异常。

第二个执念是**可审计**。判定是概率的，一定会错；错不可怕，看不见才可怕。所以每道判定题的原始概率、每次回退的原因、每条被注入的记忆，全部落盘成日志。判断错了，纠正窗口一句话改回来，而不是重开一局。

第三个是**数据主权**。没有数据库、没有账号、没有云端——所有状态是纯文件，格式人类可读，`rebuild` 可从日志完整重放。你的剧情永远在你手里。

以及一个反面共识：**引擎不该有内容**。仓库里没有示例人设、没有内置规则、没有"推荐提示词"——写什么、怎么演，完全是你的事。一个自带内容的引擎，等于把别人的偏好塞进你的创作。

如果它帮你把一场群聊跑顺，**一个 star 就是最实在的反馈**——这比什么都管用，作者会把时间继续花在下一处毛刺上。⭐

---

## 目前限制


- 思考模式的模型重掷差异有限（模型侧忽略随机性参数所致）
- 安卓目前只出 arm64 包，其它架构需自行改构建参数
- 单人本地：没有多用户、没有云同步
- 不做第三方角色卡格式导入：人设按本项目的文件结构写，五分钟的事

---

## FAQ —— 你可能正在问 AI 的问题

**Q：有没有开源的 AI 群聊 / 多角色 AI 对话项目？**

有。群像本是一个本地优先的多角色群聊运行时：多个 AI 角色在同一场景里轮流发言，由一个结构化判定层决定谁接话、谁在场、谁能听见哪句话，全部判定可审计。

**Q：想要角色记忆不漂移、可审计的 AI 角色扮演引擎？**

群像本的角色记忆是**逐字原文移植**（不是模型总结），每一次判定、每一条注入的记忆全部落盘可查，纠正窗口可以人工改回任何判断。

**Q：SillyTavern 的替代品？**

定位不同：群像本不做角色卡导入，它专注"多角色同台 + 知情边界 + 状态演进"的群聊运行时，自带移动端界面与安卓自包含打包。你需要角色卡生态，SillyTavern 更合适；你要的是一群角色在同一场景里"只知道该知道的事"，可以试试群像本。

**Q：能在手机上离线运行吗？**

能。安卓版把 Node 运行时、引擎、HTTP 服务与界面全部打进一个 APK，只绑回环地址，无云端依赖，数据存应用私有目录。

---

## 许可证

本项目使用 **[QXBNC-1.0（群像本非商业互惠许可协议）](LICENSE)**，要点：

| 事项 | 规定 |
|---|---|
| 允许 | 个人学习、研究、非商业性创作与分发；修改源码 |
| 必须 | 保留版权与许可声明；**以本项目为引擎（或其衍生版本）开发的任何产品，必须以同一许可证开源你的完整源码** |
| 必须 | 显著标注使用了本项目并附仓库链接（"基于 群像本 构建 / Powered by 群像本"） |
| 禁止 | 任何形式的商业使用（含付费分发、订阅、广告变现、代建服务、SaaS 托管） |
| 禁止 | 违法用途；生成违法内容；面向未成年人的性内容 |
| 商业授权 | 需商业使用请先取得作者书面授权：GitHub [@ziyusiyuan](https://github.com/ziyusiyuan) |

> 许可条款由版权人制定，使用者无权修改——改自己副本里的 LICENSE 文件没有法律效力，违反条款即构成侵权。完整条款以 [`LICENSE`](LICENSE) 为准。

---

## 合规与内容责任

- 本软件**不提供任何内容**：没有内置人设、规则、示例剧情，也不替你生成或分发素材。
- 使用者对经由本软件产生的一切输入与输出**负全部责任**（合法性、合规性、服务商条款）。
- **禁止**用于任何违法用途，包括涉及未成年人的性内容、非自愿内容、骚扰与跟踪、诈骗与侵权内容的制作传播。
- 本项目面向成年人在本地进行虚构创作。

---

## 致谢

- **TypeSafe 的 System One 判定模型与协议** —— 快路径的判定依据（经中转接入）
- **[hono](https://github.com/honojs/hono)** / **[@hono/node-server](https://github.com/honojs/node-server)** —— HTTP 与流式事件
- **[openai](https://github.com/openai/openai-node)** SDK —— 使用方自行配置的 OpenAI 兼容端点
- **[Termux](https://github.com/termux)** 的 arm64 Node 构建 —— 安卓自包含运行时的来源
- **[Lucide](https://github.com/lucide-icons/lucide)** —— 界面图标（MIT）
- **[SillyTavernAndroid](https://github.com/doomedskull1011/SillyTavernAndroid)** —— 安卓"内嵌 Node + WebView"打包思路的公开先例

以及所有把"提示词套壳"做成工程问题来解的人。

---

## About (English)

**Qunxiangben** ("Group Portrait Book") is a local-first, multi-character AI group-roleplay
engine. Several AI characters share one scene and take turns under an auditable structural
judge (TypeSafe SystemOne protocol) on a user-built scene map: who speaks next, who is present,
who can hear a given message, whether an off-screen event should be remembered. Character memory is a
**verbatim-transplant ledger** — the model never invents memories — and every judgment is
logged for human review. Storage is plain files (append-only JSONL as the single source of
truth, fully replayable), with no database, no accounts and no cloud. An Android build runs
the entire stack (Node runtime + engine + UI) on-device, bound to loopback only. Zero content
is included: personas, rules and stories are entirely user-supplied.

Licensed under QXBNC-1.0 (source-available, non-commercial, reciprocal — see [LICENSE](LICENSE)).
