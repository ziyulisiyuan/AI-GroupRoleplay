# 前端设计说明（web/ · 微信风格移动端）

> 覆盖 `web/` 全部前端与按 SPEC §7.5 流程接入的端点（头像 / 场景）。后端引擎的业务逻辑
> （src/group/*）不在样式工作范围内；任何新端点先更新 SPEC §7 再实现。

## Brief

```
Users: 项目主人本人        Scenario: 局域网/内网穿透，手机（Android）优先，PC 兼用
Mood: 微信原生、干净、跟手   Brand: 微信绿（#07C160 / 气泡 #95EC69）
Stack: 既有 React 18 + Vite，零新增运行时依赖（流畅度硬约束）
Scope: web/ 全部前端
```

## 一、设计 token（web/src/tokens.css，组件只允许引用语义名）

| 语义 | 值 | 用途 |
|---|---|---|
| `--bg-page` | `#f7f7f7` | 导航/TabBar/表单页底 |
| `--bg-chat` | `#ededed` | 聊天滚动区底 |
| `--bg-cell` / `--bg-press` | `#ffffff` / `#ececec` | 列表单元 / 按住态 |
| `--text-1/2/3` | `rgba(0,0,0,.9)` / `#888` / `#b2b2b2` | 正文 / 注释 / 弱提示（正文一律 text-1，保证 ≥4.5:1） |
| `--brand` / `--brand-press` | `#07c160` / `#06ad56` | 激活 Tab、主按钮、文本操作 |
| `--bubble-out` | `#95ec69` | 自己的气泡（黑字，对比 ≈13:1） |
| `--link` / `--danger` | `#576b95` / `#fa5151` | 次级操作 / 删除 |
| `--line` | `#e7e7e7`（scaleY(.5) 发丝线） | 分隔 |
| 圆角 | 5/6/8/16 px | 头像 / 按钮·输入 / 气泡·单元 / 弹层顶角 |
| 间距 | 4 的倍数（--s-1…--s-6） | 全部留白取值来源 |
| 字号 | 17 / 16 / 14 / 12 px | 气泡正文 / 单元 / 注释 / 弱提示 |
| 结构 | 导航 44 + TabBar 50 + 单元 54 px + 安全区 env() | |
| 动效 | 0.06 / 0.18 / 0.22 s，键盘专用 `cubic-bezier(.25,1,.5,1)` | **只动 transform/opacity** |

**亮色单主题是刻意决策**：对齐参考截图（微信亮色），避免半成品暗色；`review-rubric` 的暗色检查项以"不适用"记录。

## 二、结构与决策

- **零新增依赖**：不用 UI 库/动画库/路由库。图标 = 从捆绑的 Lucide 官方集内联 SVG（MIT）；页面切换 = 自制视图栈 + 一段 0.18s 入场动画；弹层/确认/Toast 各一个小组件。理由：动画复杂→手机卡顿，且包体最终 gzip ≈ 60KB。
- **移动优先**：base 样式即手机；≥768px 收成 520px 居中列（`min-width` 增强式，无 max-width 覆盖）。
- **键盘处理（零 JS 位移）**：输入框是普通文档流元素，钉在页面底部；"键盘把页面顶矮"的浏览器天然把它抬到键盘上沿；一个极小的 visualViewport 监听只负责防整页上推（滚回 0）和贴底。已知取舍：iOS Safari 这类"键盘悬浮、页面不缩"的浏览器会盖住输入框——目标设备组合是 Android+PC，接受。
- **发送时序的掩盖（乐观回显 + 整轮暂存）**：引擎按 SPEC §1.1 要先过 Jev 快路径判定、拿到知情名单后才把用户消息落盘，这段时间流里没有"你的消息"（后端时序是知情快照出生即定的刻意设计，不改）。前端做法：① 点发送瞬间把这句话以同款绿泡挂上消息流；② **整轮暂存数组**：speaker 追加一条、delta 往最后一条写、reply 只标记完成不删除——接力 `我>B>C` 期间顺序单调递增，不会出现"我被复读一遍"或 B 闪没；③ 流结束快照刷新（真实消息全在内）后的同一次渲染提交里整体撤下暂存与回显气泡。发送失败立即撤下并报错。
- **聊天气泡**：自己=右侧绿泡带头像，角色=左侧白泡带名字标签（群聊语义）；泡尾用 8px 旋转方块伪元素（签名细节）。不显示时间戳。聊天栏只有消息+流式文本，route/ledger/info 走输入框上方单行瞬态状态（SPEC §7.5 决策）。
- **消息操作 = 长按 450ms**（桌面右键等效触发 contextmenu，位移 >10px 取消）：修改 / 删除 / 批量删除，最后一条角色消息另有"重掷这条回复"。批量删除进入多选模式：行侧复选框、点选切换、输入条换成 取消/删除(N) 操作条，一次确认（文案按 SPEC 说明"日志不保留原文"）后逐条物理删除。流式结束读尽 NDJSON 后重拉快照；后台记账晚于流关闭，快照/判定日志重拉规则保留。
- **"···"→聊天信息页**（对齐微信"聊天信息"页形态，内容多的全部用页面不用弹窗）：顶部群头像+成员头像行（点像直达），下分七页：群聊设定 / 我的设定 / 纠正窗口（真气泡对话：你=右绿泡、总管=左白泡带名字）/ **场景（地图）** / 角色（列表→资料五字段+记忆面板+头像；编辑页只读展示目前所在场景——位置表派生）/ 状态账本（七字段手风琴，整体快照保存）/ 运行日志（判定.jsonl 尾部，行展开原始 JSON）。成员的现场资格完全由位置表派生，没有单独的现场勾选页。
- **全站不放灰色说明小字**：界面保持自明；字段名保留最小语义（称呼/你的设定/总管基调）；placeholder 只当输入提示用。
- **地图（SPEC §4）**：聊天信息→场景页 = 添加场景横条（悬浮弹窗表单）+ 场景列表（名称+描述，行内（当前）标记；名称一经创建不可改不可删，描述可改）；新建群聊页随建图（场景条目 + 点选开局地点）；新建角色表单带初始所在场景（建时选定，之后只读）；聊天输入条左侧定位针按钮 = 场景手选（悬浮弹窗列出全部场景，选中后本轮发送直接按"已移动"处理，跳过换场景判定）。
- **头像系统**：纯显示、永不进模型输入。上传 = 前端 canvas 居中裁方 320px JPEG（~15-30KB）→ PUT 裸字节；后端魔数校验（jpeg/png/webp/gif）+ 2MB 上限，存 `头像.dat` / `用户头像.dat`；未设置 404 → 回退头像（名字首字 + 名字哈希调色板底色），布局不因有无头像而变化。端点契约见 SPEC §7.2。
- **内网穿透准备**：`vite.config.ts` 加 `host: true`（dev 服监听 0.0.0.0，手机连 `http://<PC IP>:5173`）；端口 5173/8787 未动，两个 .bat 不受影响。生产式构建产物 `web/dist` 由 `pnpm web:build` 生成，日常手机访问建议直接用 dev 服（代理已配好）。

## 三、镜像契约核对（SPEC §7.5）

- `api.ts` 的 `Ev` ↔ `SessionEvent`、`Snapshot`（含 `scene`/`scenes`）↔ `snapshot()`、`LEDGER_KEYS`（groupinfo.tsx，七字段一字不差）✔
- 来源标签（亲历/额外得知/现场所见/用户指定/推断/他人告知）、层名（现场/接入/单向感知）、感知值（语音/视听）逐字显示 ✔（在场页、记忆面板）
- 删除确认说明物理删除 ✔；场景页不渲染 `absent` ✔；模型页只改密钥 ✔；流后重拉快照与判定日志 ✔

## 四、自评（review-rubric 七维，2 分制）

```
Score: contrast 2 hierarchy 2 rhythm 2 whitespace 2 consistency 2 responsive 2 restraint 2 = 14/14
说明：contrast——正文 rgba(0,0,0,.9) 全部 ≥4.5:1；#888/#b2b2b2 仅用于 12-14px 注释
（微信同款处理，不承载正文）。rhythm——列表页的"头像+发丝线单元"是平台原生语法并
贯穿主页/信息页/子页（签名元素回声），聊天页气泡两侧交替+时间条提供变化。
responsive——375/1280 视口实截核对通过（截图属一次性工件，不入库、不留盘），
768 为二者间平稳过渡；无横向滚动（泡文 word-break）。
诚实记录的剩余问题：
1) 键盘动画只做了 --kb 模拟验证，iOS Safari 连续 resize / 微信内置浏览器未真机验证
   ——若 iOS 上回落有残影，首选把 --t-kb 降到 0.12s；
2) 暗色主题未做（刻意决策，见上）；
3) 从聊天进信息页再返回，聊天滚动位置会回到底部（未做位置保持，数据无损）。
```

## 五、参考来源

- 视觉基准：微信移动端截图（列表页 / 聊天页 / 键盘顶起 / 聊天信息页）
- 图标：捆绑引擎库 `engines\icons\*.svg`（Lucide 官方，MIT），内联其路径数据，未外链任何图片
- SPEC：`docs/SPEC.md` §3 / §4 / §7 / §7.5 / §8

## 六、运行

```
双击 启动.bat（或 pnpm host + pnpm web:dev）
PC:      http://localhost:5173
手机:    http://<电脑局域网IP>:5173   （vite 已 host:true）
```

自审截图与验证脚本属一次性工件：不入库（`.gitignore` 已排除 `.shots/`），验证后清除，
避免把界面里的对话内容留在磁盘上。
